name: Build and Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Build with pspdev
        run: |
          docker run --rm -v "$PWD:/build" pspdev/pspdev:latest \
            bash -c "psp-pacman -Sy --noconfirm sdl2 && cd /build && make"

      - name: Package release
        run: |
          mkdir -p release/PAPER-PSP
          cp EBOOT.PBP release/PAPER-PSP/
          cp LICENSE release/PAPER-PSP/
          cp -r .screenshots release/PAPER-PSP
          cp README.md release/PAPER-PSP/
          cd release && zip -r ../paper-psp-${{ github.ref_name }}.zip .

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: paper-psp-*.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}